##############################################################################
# CD Pipeline — IBM Cloud Projects Deployment
# Repo: nava-dba/terraform-ibm-oracle-powervs-da
##############################################################################
name: CD-Pipeline

on:
  workflow_dispatch:
    inputs:
      variation:
        description: "Deployment variation"
        required: true
        type: choice
        options:
          - si
          - rac
      action:
        description: "Action to perform"
        required: true
        type: choice
        options:
          - validate-only
          - validate-and-deploy
          - destroy

env:
  IBM_API_URL: "https://projects.api.cloud.ibm.com/v1"

jobs:

  ##############################################################################
  # JOB 1 — Validate
  ##############################################################################
  validate:
    name: Validate (${{ github.event.inputs.variation }})
    runs-on: ubuntu-latest
    if: github.event.inputs.action != 'destroy'

    outputs:
      config_id: ${{ steps.set-ids.outputs.config_id }}

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Login and Get IAM Token
        run: |
          curl -fsSL https://clis.cloud.ibm.com/install/linux | sh -s -- -q
          ibmcloud login --apikey "${{ secrets.IC_API_KEY }}" --no-region -q
          ibmcloud target -r us-south -q
          TOKEN=$(ibmcloud iam oauth-tokens | grep "IAM token" | awk '{print $4}')
          echo "IAM_TOKEN=${TOKEN}" >> $GITHUB_ENV
          echo "IAM token obtained"

      - name: Set Config ID
        id: set-ids
        run: |
          if [[ "${{ github.event.inputs.variation }}" == "si" ]]; then
            echo "config_id=${{ secrets.IBM_SI_CONFIG_ID }}" >> $GITHUB_OUTPUT
          else
            echo "config_id=${{ secrets.IBM_RAC_CONFIG_ID }}" >> $GITHUB_OUTPUT
          fi

      - name: Build Full Inputs
        run: |
          python3 -c '
          import json, os
          with open(os.environ["INPUTS_FILE"]) as f:
              inputs = json.load(f)
          inputs["ibmcloud_api_key"] = os.environ["TF_VAR_IBMCLOUD_API_KEY"]  # pragma: allowlist secret
          inputs["ssh_private_key"] = os.environ["TF_VAR_SSH_PRIVATE_KEY"]  # pragma: allowlist secret
          inputs["ora_db_password"] = os.environ["TF_VAR_ORA_DB_PASSWORD"]  # pragma: allowlist secret
          inputs["ibmcloud_cos_service_credentials"] = os.environ["TF_VAR_IBMCLOUD_COS_SERVICE_CREDENTIALS"]  # pragma: allowlist secret
          if os.environ["VARIATION"] == "rac":
              inputs["root_password"] = os.environ.get("TF_VAR_ROOT_PASSWORD", "")  # pragma: allowlist secret
          with open("/tmp/ibm_inputs.json", "w") as out:
              json.dump({"definition": {"inputs": inputs}}, out, indent=2)
          print("Inputs built: " + str(len(inputs)) + " keys")
          '
        env:
          TF_VAR_IBMCLOUD_API_KEY: ${{ secrets.TF_VAR_IBMCLOUD_API_KEY }}
          TF_VAR_SSH_PRIVATE_KEY: ${{ secrets.TF_VAR_SSH_PRIVATE_KEY }}
          TF_VAR_ORA_DB_PASSWORD: ${{ secrets.TF_VAR_ORA_DB_PASSWORD }}
          TF_VAR_ROOT_PASSWORD: ${{ secrets.TF_VAR_ROOT_PASSWORD }}
          TF_VAR_IBMCLOUD_COS_SERVICE_CREDENTIALS: ${{ secrets.TF_VAR_IBMCLOUD_COS_SERVICE_CREDENTIALS }}
          INPUTS_FILE: deploy/${{ github.event.inputs.variation }}/inputs.json
          VARIATION: ${{ github.event.inputs.variation }}

      - name: Update Config Inputs
        run: |
          CONFIG_ID="${{ steps.set-ids.outputs.config_id }}"
          RESPONSE=$(curl -s -w "\n%{http_code}" -X PATCH \
            "${{ env.IBM_API_URL }}/projects/${{ secrets.IBM_PROJECT_ID }}/configs/${CONFIG_ID}" \
            -H "Authorization: Bearer ${IAM_TOKEN}" \
            -H "Content-Type: application/json" \
            -d @/tmp/ibm_inputs.json)
          HTTP_CODE=$(echo "$RESPONSE" | tail -1)
          BODY=$(echo "$RESPONSE" | head -1)
          echo "HTTP: ${HTTP_CODE}"
          if [[ "$HTTP_CODE" != "200" ]]; then
            echo "Update failed:"
            echo "$BODY" | python3 -m json.tool 2>/dev/null || echo "$BODY"
            exit 1
          fi
          echo "Config inputs updated"

      - name: Trigger Validation
        run: |
          CONFIG_ID="${{ steps.set-ids.outputs.config_id }}"
          MAX_RETRIES=5
          RETRY_DELAY=30
          for i in $(seq 1 $MAX_RETRIES); do
            RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
              "${{ env.IBM_API_URL }}/projects/${{ secrets.IBM_PROJECT_ID }}/configs/${CONFIG_ID}/validate" \
              -H "Authorization: Bearer ${IAM_TOKEN}" \
              -H "Content-Type: application/json" \
              -d '{}')
            HTTP_CODE=$(echo "$RESPONSE" | tail -1)
            BODY=$(echo "$RESPONSE" | head -1)
            echo "Attempt ${i}: HTTP ${HTTP_CODE}"
            if [[ "$HTTP_CODE" == "200" || "$HTTP_CODE" == "202" ]]; then
              echo "Validation triggered"
              exit 0
            elif [[ "$HTTP_CODE" == "500" || "$HTTP_CODE" == "429" ]]; then
              echo "Rate limited or transient error — waiting ${RETRY_DELAY}s before retry..."
              sleep $RETRY_DELAY
              RETRY_DELAY=$((RETRY_DELAY * 2))
            else
              echo "Trigger validation failed:"
              echo "$BODY" | python3 -m json.tool 2>/dev/null || echo "$BODY"
              exit 1
            fi
          done
          echo "Trigger validation failed after ${MAX_RETRIES} attempts"
          exit 1

      - name: Poll Validation Status
        run: |
          CONFIG_ID="${{ steps.set-ids.outputs.config_id }}"
          MAX_WAIT=900
          INTERVAL=30
          ELAPSED=0
          echo "Polling validation (max 30 min)..."

          while [ $ELAPSED -lt $MAX_WAIT ]; do
            RESPONSE=$(curl -s \
              "${{ env.IBM_API_URL }}/projects/${{ secrets.IBM_PROJECT_ID }}/configs/${CONFIG_ID}" \
              -H "Authorization: Bearer ${IAM_TOKEN}")
            STATE=$(echo "$RESPONSE" | python3 -c 'import json,sys; d=json.load(sys.stdin); print(d.get("state","unknown"))')
            echo "[${ELAPSED}s] State: ${STATE}"

            if [[ "$STATE" == "validated" ]]; then
              echo "Validation passed!"
              exit 0
            elif [[ "$STATE" == "validating_failed" ]]; then
              echo "Validation failed:"
              echo "$RESPONSE" | python3 -c '
          import json, sys
          d = json.load(sys.stdin)
          for item in d.get("needs_attention_state", []):
              print(item)
          '
              exit 1
            fi

            sleep $INTERVAL
            ELAPSED=$((ELAPSED + INTERVAL))
          done

          echo "Validation timed out after ${MAX_WAIT}s"
          exit 1

  ##############################################################################
  # JOB 2 — Deploy
  ##############################################################################
  deploy:
    name: Deploy (${{ github.event.inputs.variation }})
    runs-on: ubuntu-latest
    needs: validate
    environment: production
    if: github.event.inputs.action == 'validate-and-deploy'

    steps:
      - name: Login and Get IAM Token
        run: |
          curl -fsSL https://clis.cloud.ibm.com/install/linux | sh -s -- -q
          ibmcloud login --apikey "${{ secrets.IC_API_KEY }}" --no-region -q
          ibmcloud target -r us-south -q
          TOKEN=$(ibmcloud iam oauth-tokens | grep "IAM token" | awk '{print $4}')
          echo "IAM_TOKEN=${TOKEN}" >> $GITHUB_ENV
          echo "IAM token obtained"

      - name: Set Config ID
        id: set-ids
        run: |
          if [[ "${{ github.event.inputs.variation }}" == "si" ]]; then
            echo "config_id=${{ secrets.IBM_SI_CONFIG_ID }}" >> $GITHUB_OUTPUT
          else
            echo "config_id=${{ secrets.IBM_RAC_CONFIG_ID }}" >> $GITHUB_OUTPUT
          fi

      - name: Approve Config
        run: |
          CONFIG_ID="${{ steps.set-ids.outputs.config_id }}"
          RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
            "${{ env.IBM_API_URL }}/projects/${{ secrets.IBM_PROJECT_ID }}/configs/${CONFIG_ID}/approve" \
            -H "Authorization: Bearer ${IAM_TOKEN}" \
            -H "Content-Type: application/json" \
            -d "{\"comment\": \"Approved via CD pipeline by ${{ github.actor }}\"}")
          HTTP_CODE=$(echo "$RESPONSE" | tail -1)
          if [[ "$HTTP_CODE" != "200" && "$HTTP_CODE" != "202" ]]; then
            echo "Approval failed (HTTP ${HTTP_CODE}):"
            echo "$RESPONSE" | head -1 | python3 -m json.tool 2>/dev/null
            exit 1
          fi
          echo "Config approved by ${{ github.actor }}"

      - name: Trigger Deploy
        run: |
          CONFIG_ID="${{ steps.set-ids.outputs.config_id }}"
          MAX_RETRIES=5
          RETRY_DELAY=30
          for i in $(seq 1 $MAX_RETRIES); do
            RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
              "${{ env.IBM_API_URL }}/projects/${{ secrets.IBM_PROJECT_ID }}/configs/${CONFIG_ID}/deploy" \
              -H "Authorization: Bearer ${IAM_TOKEN}" \
              -H "Content-Type: application/json" \
              -d '{}')
            HTTP_CODE=$(echo "$RESPONSE" | tail -1)
            BODY=$(echo "$RESPONSE" | head -1)
            echo "Attempt ${i}: HTTP ${HTTP_CODE}"
            if [[ "$HTTP_CODE" == "200" || "$HTTP_CODE" == "202" ]]; then
              echo "Deploy triggered"
              exit 0
            elif [[ "$HTTP_CODE" == "409" ]]; then
              echo "Config is already deploying (auto-deploy may be enabled) — skipping trigger, polling instead"
              exit 0
            elif [[ "$HTTP_CODE" == "500" || "$HTTP_CODE" == "429" ]]; then
              echo "Rate limited or transient error — waiting ${RETRY_DELAY}s before retry..."
              sleep $RETRY_DELAY
              RETRY_DELAY=$((RETRY_DELAY * 2))
            else
              echo "Deploy trigger failed:"
              echo "$BODY" | python3 -m json.tool 2>/dev/null || echo "$BODY"
              exit 1
            fi
          done
          echo "Deploy trigger failed after ${MAX_RETRIES} attempts"
          exit 1

      - name: Poll Deploy Status
        run: |
          CONFIG_ID="${{ steps.set-ids.outputs.config_id }}"
          MAX_WAIT=$([ "${{ github.event.inputs.variation }}" == "si" ] && echo 10800 || echo 21600)
          INTERVAL=900
          ELAPSED=0
          echo "Polling deploy status (max 6 hours for rac and 3 hrs for si)..."

          while [ $ELAPSED -lt $MAX_WAIT ]; do
            RESPONSE=$(curl -s \
              "${{ env.IBM_API_URL }}/projects/${{ secrets.IBM_PROJECT_ID }}/configs/${CONFIG_ID}" \
              -H "Authorization: Bearer ${IAM_TOKEN}")
            STATE=$(echo "$RESPONSE" | python3 -c 'import json,sys; d=json.load(sys.stdin); print(d.get("state","unknown"))')
            echo "[${ELAPSED}s] State: ${STATE}"

            if [[ "$STATE" == "deployed" ]]; then
              echo "Deployment successful!"
              echo "$RESPONSE" | python3 -c '
          import json, sys
          d = json.load(sys.stdin)
          print("--- Outputs ---")
          for o in d.get("outputs", []):
              print(o.get("name"), ":", o.get("value", "N/A"))
          '
              exit 0
            elif [[ "$STATE" == "deploying_failed" ]]; then
              echo "Deployment failed!"
              exit 1
            fi

            sleep $INTERVAL
            ELAPSED=$((ELAPSED + INTERVAL))
          done

          echo "Deploy timed out after ${MAX_WAIT}s"
          exit 1

  ##############################################################################
  # JOB 3 — Destroy
  ##############################################################################
  destroy:
    name: Destroy (${{ github.event.inputs.variation }})
    runs-on: ubuntu-latest
    environment: production
    if: github.event.inputs.action == 'destroy'

    steps:
      - name: Login and Get IAM Token
        run: |
          curl -fsSL https://clis.cloud.ibm.com/install/linux | sh -s -- -q
          ibmcloud login --apikey "${{ secrets.IC_API_KEY }}" --no-region -q
          ibmcloud target -r us-south -q
          TOKEN=$(ibmcloud iam oauth-tokens | grep "IAM token" | awk '{print $4}')
          echo "IAM_TOKEN=${TOKEN}" >> $GITHUB_ENV
          echo "IAM token obtained"

      - name: Set Config ID
        id: set-ids
        run: |
          if [[ "${{ github.event.inputs.variation }}" == "si" ]]; then
            echo "config_id=${{ secrets.IBM_SI_CONFIG_ID }}" >> $GITHUB_OUTPUT
          else
            echo "config_id=${{ secrets.IBM_RAC_CONFIG_ID }}" >> $GITHUB_OUTPUT
          fi

      - name: Trigger Undeploy
        run: |
          CONFIG_ID="${{ steps.set-ids.outputs.config_id }}"
          MAX_RETRIES=5
          RETRY_DELAY=30
          for i in $(seq 1 $MAX_RETRIES); do
            RESPONSE=$(curl -s -w "\n%{http_code}" -X POST \
              "${{ env.IBM_API_URL }}/projects/${{ secrets.IBM_PROJECT_ID }}/configs/${CONFIG_ID}/undeploy" \
              -H "Authorization: Bearer ${IAM_TOKEN}" \
              -H "Content-Type: application/json" \
              -d '{}')
            HTTP_CODE=$(echo "$RESPONSE" | tail -1)
            BODY=$(echo "$RESPONSE" | head -1)
            echo "Attempt ${i}: HTTP ${HTTP_CODE}"
            if [[ "$HTTP_CODE" == "200" || "$HTTP_CODE" == "202" ]]; then
              echo "Undeploy triggered"
              exit 0
            elif [[ "$HTTP_CODE" == "500" || "$HTTP_CODE" == "429" ]]; then
              echo "Rate limited or transient error — waiting ${RETRY_DELAY}s before retry..."
              sleep $RETRY_DELAY
              RETRY_DELAY=$((RETRY_DELAY * 2))
            else
              echo "Undeploy trigger failed:"
              echo "$BODY" | python3 -m json.tool 2>/dev/null || echo "$BODY"
              exit 1
            fi
          done
          echo "Undeploy trigger failed after ${MAX_RETRIES} attempts"
          exit 1

      - name: Poll Destroy Status
        run: |
          CONFIG_ID="${{ steps.set-ids.outputs.config_id }}"
          MAX_WAIT=3600
          INTERVAL=60
          ELAPSED=0
          echo "Polling destroy status (max 3 hours)..."

          while [ $ELAPSED -lt $MAX_WAIT ]; do
            RESPONSE=$(curl -s \
              "${{ env.IBM_API_URL }}/projects/${{ secrets.IBM_PROJECT_ID }}/configs/${CONFIG_ID}" \
              -H "Authorization: Bearer ${IAM_TOKEN}")
            STATE=$(echo "$RESPONSE" | python3 -c 'import json,sys; d=json.load(sys.stdin); print(d.get("state","unknown"))')
            echo "[${ELAPSED}s] State: ${STATE}"

            if [[ "$STATE" == "undeployed" || "$STATE" == "validated" ]]; then
              echo "Resources destroyed successfully!"
              exit 0
            elif [[ "$STATE" == "undeploying_failed" ]]; then
              echo "Destroy failed!"
              exit 1
            fi

            sleep $INTERVAL
            ELAPSED=$((ELAPSED + INTERVAL))
          done

          echo "Destroy timed out after ${MAX_WAIT}s"
          exit 1
